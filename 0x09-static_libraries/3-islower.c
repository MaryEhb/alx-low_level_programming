#include "main.h"

/**
 * _islower - check if character is lowercase
 * @c: the character to be checked
 * Return: 1 if lower, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
